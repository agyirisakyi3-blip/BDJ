name: CI

on:
  push:
    branches: [main, master, v2-supabase]
  pull_request:

jobs:
  backend-tests:
    name: Backend tests (Jest + Puppeteer)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: npm audit (root)
        run: npm audit --omit=dev
        # Advisory only for now; tighten to blocking once deps are clean.
        continue-on-error: true

  frontend:
    name: Frontend lint + production build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build (production)
        run: npm run build

      - name: npm audit (frontend)
        run: npm audit
        continue-on-error: true